[project]
name = "capacity-connect"
version = "0.1.0"
description = "A simple FastAPI backend for Capacity Connect"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "google-genai>=2.22.0",
    "uvicorn>=0.52.4",
]
