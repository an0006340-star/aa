{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run --project ../.. uvicorn main:app --host 0.0.0.0 --port ${PORT:-5000}",
    "build": "uv run --project ../.. python -m compileall main.py",
    "start": "uv run --project ../.. uvicorn main:app --host 0.0.0.0 --port ${PORT:-5000}"
  }
}
